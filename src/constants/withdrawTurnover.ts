/** 「提现流水」字段展示名 */
export const WITHDRAW_TURNOVER_LABEL = '提现流水'

/** 按场景拆分的「提现流水」需求说明（用于 wf-spec-annot） */
export const WITHDRAW_TURNOVER_SPEC = {
  table: [
    '展示本笔账变关联的提现流水变动数值。',
    '正数表示增加提现流水，负数表示扣减（列表以 +/- 或颜色区分）。',
    '与账变金额可独立填写，由运营按实际业务记录。',
    '未填写时列表可能展示为 0 或 —，以后端规则为准。',
  ],
  form: [
    '选填；用于记录用户提现侧流水变动，提交后写入列表「提现流水」列。',
    '支持小数；负数表示减少提现流水。',
    '与账变金额无自动换算关系，需人工核对后填写。',
    '留空时默认按 0 处理（演示数据）。',
  ],
  detail: [
    '与列表「提现流水」列一致，展示该笔账变关联的提现流水变动。',
    '正数为增加提现流水，负数为扣减；符号/颜色规则与列表相同。',
    '详情为只读展示，修改需通过账变管理重新发起。',
  ],
  audit: [
    '风控侧对提现流水变动申请的独立审核列表，与账变审核流程分离。',
    '审核通过后提现流水才生效；驳回须填写理由并通知发起人。',
    '列表「提现流水」列含义与账变管理一致，正数增加、负数扣减。',
  ],
} as const

export type WithdrawTurnoverSpecContext = keyof typeof WITHDRAW_TURNOVER_SPEC
