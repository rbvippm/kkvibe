---
description: KK Vibe H5 移动端 App 壳层规范，基于 /mobile 路由、MobileAppLayout 与 mobile-app-shell.css
globs: src/layouts/MobileAppLayout.vue,src/views/mobile/**/*.vue,src/views/AgentView.vue,src/components/mobile/**/*.vue,src/styles/mobile-app-shell.css,src/constants/mobilePrdSpec.ts,docs/H5移动端设计规范.md
alwaysApply: false
---

# H5 移动端规范

开发 `/mobile/*` 页面时，遵循 `docs/H5移动端设计规范.md` 与 `src/styles/mobile-app-shell.css`。

## 页面结构

- `/mobile` 统一走 `MobileAppLayout`，业务页不要重复实现 `mh5-viewport-canvas`、`mh5-app-shell` 或一级底栏。
- 一级 Tab：大厅、社区、会话、我的，由 `MobileAppLayout` 维护。
- 二级业务页配置 `meta.hideTabBar: true`，优先使用 `Mh5SubPageHeader`。
- `/mobile/agent` 使用 `AgentView` + `AgentBottomNav` 内部五 Tab。

## 必做 / 禁止

- 必做：触控热区 ≥44px、safe-area、中文 Mock、空态 / 异常态、页面文案与 Mock 一致。
- 页面有「注N」标注时，必须同步维护 `MobilePrdSpec`，原型和 PRD 双向一致。
- 禁止：PC 线框 `wf-*`、`WfSpecAnnot`、PC 的 `items[]` 简版标注结构。

## 标准样例

- App 壳层：`src/layouts/MobileAppLayout.vue`
- 大厅：`src/components/mobile/MobileLobbyPage.vue`
- 代理中心：`src/views/AgentView.vue`
- 二级页头：`src/components/mobile/Mh5SubPageHeader.vue`
